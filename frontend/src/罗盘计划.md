````markdown
你现在是我的高级 GIS / WebGIS 架构 Agent。

你需要为我的 WebGIS 项目新增一个：

# 「风水罗盘交互矢量图层系统」

目标不是简单 Canvas 高亮。

而是：

```text
把风水罗盘中的每一个扇区
真正矢量化（Feature 化）
并接入我的 GIS 图层体系
````

最终效果：

```text
用户绘制罗盘
→ 罗盘扇区自动转换为 OpenLayers Vector Features
→ 自动生成 VectorLayer
→ 自动加入 TOCPanel
→ 用户点击 TOC 可控制显示隐藏
→ 地图点击扇区可高亮
→ 弹出风水信息
→ 支持 hover / select / analysis
```

这是一个：

```text
Canvas → GIS Feature Layer
```

的架构升级。

---

# 一、我的项目技术栈

---

核心：

* Vue3
* OpenLayers
* Pinia
* Vite

已有系统：

* TOCPanel.vue
* useLayerStore.ts
* useTOCStore.ts
* useCreateManagedVectorLayer.js
* useManagedLayerRegistry.js
* useManagedFeatureHighlight.js
* useUserLayerApiFacade.js
* CompassManager.ts
* feng-shui-compass-svg/

系统已经具备：

```text
托管图层体系
TOC树体系
Feature高亮体系
Layer管理体系
```

因此：

你不能重新发明一套图层系统。

必须：

```text
完全接入现有架构
```

---

# 二、核心目标（非常重要）

---

不要再把罗盘当作：

```text
纯 SVG / Canvas 图形
```

而是：

```text
空间对象系统
```

即：

每一个扇区：

* 都是 Feature
* 都有 Geometry
* 都有 Attributes
* 都能被查询
* 都能被高亮
* 都能进入 TOC
* 都能参与 GIS 分析

---

# 三、最终架构要求

---

必须实现：

# 1. 风水扇区 Feature 化

每个扇区必须生成：

```js
Feature({
    geometry,
    name,
    type,
    direction,
    layerIndex,
    sectorIndex,
    fengShuiInfo
})
```

geometry 必须是真实 Polygon。

不能只是记录角度。

---

# 2. 自动生成 VectorLayer

要求：

使用：

```text
src/composables/map/features/useCreateManagedVectorLayer.js
```

接入现有托管图层系统。

禁止手写野生 layer 管理。

---

# 3. 自动加入 TOCPanel

必须自动：

* 注册到 useTOCStore
* 出现在 TOCPanel.vue
* 支持显示隐藏
* 支持透明度
* 支持选中
* 支持右键菜单

必须遵循：

```text
src/composables/map/toc/
```

中的协议。

---

# 4. 地图点击交互

点击扇区：

必须：

* Feature 高亮
* Feature glow
* 属性弹窗
* 风水说明

必须复用：

```text
useManagedFeatureHighlight.js
```

不能重复造轮子。

---

# 5. GIS 化

这是最关键的。

罗盘必须真正：

```text
GIS Feature 化
```

意味着：

后面必须支持：

* 空间查询
* Select
* Hover
* Buffer
* Export GeoJSON
* 空间分析
* Feature Filter
* 属性查询
* AI 分析

---

# 四、你需要分析的现有系统

---

重点分析：

# 罗盘系统

```text
src/services/CompassManager.ts
src/components/feng-shui-compass-svg/
```

理解：

* 当前绘制方式
* 扇区数据来源
* SVG 结构
* 扇区角度
* 圆环结构

---

# 图层系统

重点分析：

```text
src/composables/map/features/useCreateManagedVectorLayer.js
src/composables/useManagedLayerRegistry.js
src/stores/useLayerStore.ts
src/stores/useTOCStore.ts
```

理解：

* 托管图层生命周期
* Layer 注册方式
* TOC 节点结构
* 图层 metadata

---

# Feature 高亮系统

重点分析：

```text
src/composables/map/features/useManagedFeatureHighlight.js
```

要求：

完全复用。

---

# 五、最终实现要求

---

你需要最终输出：

# 1. 完整架构设计

包括：

* Feature 生成流程
* Layer 生命周期
* TOC 同步流程
* 交互事件流

---

# 2. 新增文件设计

例如：

```text
src/services/CompassFeatureLayerService.ts
src/composables/map/features/useCompassFeatureLayer.ts
src/utils/gis/compassSectorPolygon.ts
```

但你需要自己判断最佳结构。

---

# 3. 真实代码实现

不是伪代码。

必须：

* 可直接运行
* 可接入现有项目
* 类型完整
* 架构清晰

---

# 4. Polygon 扇区生成算法

最关键。

必须：

把：

```text
角度 + 半径
```

转换为：

```text
真实 Polygon
```

用于 OpenLayers Feature。

必须考虑：

* 内半径
* 外半径
* startAngle
* endAngle
* 曲线拟合
* segment 精度

---

# 5. TOC 自动注册

必须：

自动：

```text
Compass Layer
→ Managed Layer
→ TOC Node
```

形成完整链路。

---

# 六、重要限制

---

禁止：

* 重新造 LayerManager
* 自己维护野生 Feature 状态
* 绕过 TOCStore
* 绕过 Managed Layer Registry
* 直接 DOM 操作 TOC
* 把 Feature 只存在内存数组里

必须：

```text
完全融入现有 GIS 架构
```

---

# 七、最终目标（本质）

---

我要的不是：

```text
一个会亮的罗盘
```

而是：

```text
真正 GIS 化的风水空间分析系统
```

本质上：

```text
风水罗盘
= 专题 GIS 图层
```

未来必须支持：

* 风水空间分析
* 飞星推演
* 方位冲煞分析
* 空间叠加分析
* AI 风水解盘
* 导出 GeoJSON
* Cesium 3D 映射

因此：

你必须从：

```text
GIS 空间系统设计
```

角度思考。

而不是：

```text
普通 SVG 组件
```
