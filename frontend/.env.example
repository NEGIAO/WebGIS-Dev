# WebGIS Frontend — 公开构建变量摘要（仅指路，Vite 不读取本目录 env）
#
# ⚠️ 前端 env 已统一收敛到仓库根目录（vite.config.js 的 envDir=仓库根）：
#   - 本地开发：改根目录 .env.local 的 VITE_*（git 忽略，开发覆盖值）
#   - 生产构建：根目录 .env 的 VITE_*（git 追踪，生产基线值）
# Vite 通过 selectiveEnvPlugin 按 mode 二选一：
#   npm run dev（mode=development）→ 只读 .env.local
#   npm run build（mode=production）→ 只读 .env
# 权威「全集清单」：../.env.example（含 L1/L2/L3 全部分层说明）
# 绝密（OAuth secret、SUPER_USER、SMTP 密码、地图 TK 等）禁止写成 VITE_*。
#
# 源码消费方式：统一经 src/config/publicRuntime.ts 派生基址，
# 业务代码不直接读 import.meta.env、不硬编码后端域名。
# 以下键值仅作参考展示，修改请去仓库根目录。
#
# 地图 TK / Cesium / 高德 Web 服务 Key / Agent Key → 管理员面板（L2）；不要写成 VITE_* 或 HF Secrets
# 详细说明：../Docs/Guide/configuration.md

# ========== [L1] 部署 / 后端地址（公开）==========
# 本地: ./
# GitHub Pages (WebGIS-Dev): /WebGIS-Dev/
VITE_BASE_URL=./

# 本地后端
VITE_BACKEND_URL=http://localhost:7860

# 瓦片代理根地址（默认同后端；线上可填 HF Space）
VITE_TILE_PROXY_BASE_URL=http://localhost:7860
# fallback | always | off
VITE_TILE_PROXY_MODE=fallback

# 可选：下载请求超时（ms）
# VITE_DOWNLOAD_REQUEST_TIMEOUT=2000000
