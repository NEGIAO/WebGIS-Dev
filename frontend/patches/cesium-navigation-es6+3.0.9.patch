diff --git a/node_modules/cesium-navigation-es6/dist/core/Utils.js b/node_modules/cesium-navigation-es6/dist/core/Utils.js
index 9c8dba7..e11c7a2 100644
--- a/node_modules/cesium-navigation-es6/dist/core/Utils.js
+++ b/node_modules/cesium-navigation-es6/dist/core/Utils.js
@@ -3,8 +3,18 @@ import { defined, Ray, Cartesian3, Cartographic, ReferenceFrame, SceneMode } fro
 
 var Utils = {}
 
-var unprojectedScratch = new Cartographic()
-var rayScratch = new Ray()
+var unprojectedScratch = null
+var rayScratch = null
+function getScratch(name) {
+    if (name === 'unprojected') {
+        if (!unprojectedScratch) unprojectedScratch = new Cartographic()
+        return unprojectedScratch
+    }
+    if (name === 'ray') {
+        if (!rayScratch) rayScratch = new Ray()
+        return rayScratch
+    }
+}
 
 /**
  * gets the focus point of the camera
@@ -31,9 +41,11 @@ Utils.getCameraFocus = function (terria, inWorldCoordinates, result) {
   if (defined(terria.trackedEntity)) {
     result = terria.trackedEntity.position.getValue(terria.clock.currentTime, result)
   } else {
-    rayScratch.origin = camera.positionWC
-    rayScratch.direction = camera.directionWC
-    result = scene.globe.pick(rayScratch, scene, result)
+    // 💡 改动点 1：通过 getScratch('ray') 获取实例后再赋值，避免 null 报错
+    var rs = getScratch('ray')
+    rs.origin = camera.positionWC
+    rs.direction = camera.directionWC
+    result = scene.globe.pick(rs, scene, result)
   }
 
   if (!defined(result)) {
@@ -44,7 +56,8 @@ Utils.getCameraFocus = function (terria, inWorldCoordinates, result) {
     result = camera.worldToCameraCoordinatesPoint(result, result)
 
     if (inWorldCoordinates) {
-      result = scene.globe.ellipsoid.cartographicToCartesian(scene.mapProjection.unproject(result, unprojectedScratch), result)
+      // 💡 改动点 2：将 unprojectedScratch 替换为 getScratch('unprojected')
+      result = scene.globe.ellipsoid.cartographicToCartesian(scene.mapProjection.unproject(result, getScratch('unprojected')), result)
     }
   } else {
     if (!inWorldCoordinates) {
diff --git a/node_modules/cesium-navigation-es6/dist/viewModels/DistanceLegendViewModel.js b/node_modules/cesium-navigation-es6/dist/viewModels/DistanceLegendViewModel.js
index 4f3c620..7cf49d0 100644
--- a/node_modules/cesium-navigation-es6/dist/viewModels/DistanceLegendViewModel.js
+++ b/node_modules/cesium-navigation-es6/dist/viewModels/DistanceLegendViewModel.js
@@ -81,7 +81,11 @@ DistanceLegendViewModel.create = function (options) {
   return result
 }
 
-var geodesic = new EllipsoidGeodesic()
+var geodesic = null
+function getGeodesic() {
+    if (!geodesic) geodesic = new EllipsoidGeodesic()
+    return geodesic
+}
 
 var distances = [
   1, 2, 3, 5,
@@ -126,8 +130,9 @@ function updateDistanceLegendCesium(viewModel, scene) {
   var leftCartographic = globe.ellipsoid.cartesianToCartographic(leftPosition)
   var rightCartographic = globe.ellipsoid.cartesianToCartographic(rightPosition)
 
-  geodesic.setEndPoints(leftCartographic, rightCartographic)
-  var pixelDistance = geodesic.surfaceDistance
+var g = getGeodesic()
+g.setEndPoints(leftCartographic, rightCartographic)
+var pixelDistance = g.surfaceDistance
 
   // Find the first distance that makes the scale bar less than 100 pixels.
   var maxBarWidth = 100
diff --git a/node_modules/cesium-navigation-es6/dist/viewModels/ZoomNavigationControl.js b/node_modules/cesium-navigation-es6/dist/viewModels/ZoomNavigationControl.js
index e776c81..a076d7b 100644
--- a/node_modules/cesium-navigation-es6/dist/viewModels/ZoomNavigationControl.js
+++ b/node_modules/cesium-navigation-es6/dist/viewModels/ZoomNavigationControl.js
@@ -72,7 +72,11 @@ ZoomNavigationControl.prototype.activate = function () {
   this.zoom(this.relativeAmount)
 }
 
-var cartesian3Scratch = new Cartesian3()
+var cartesian3Scratch = null
+function getCartesian3Scratch() {
+    if (!cartesian3Scratch) cartesian3Scratch = new Cartesian3()
+    return cartesian3Scratch
+}
 
 ZoomNavigationControl.prototype.zoom = function (relativeAmount) {
   // this.terria.analytics.logEvent('navigation', 'click', 'zoomIn');
@@ -133,7 +137,7 @@ ZoomNavigationControl.prototype.zoom = function (relativeAmount) {
           return
         }
 
-        var direction = Cartesian3.subtract(camera.position, focus, cartesian3Scratch)
+        var direction = Cartesian3.subtract(camera.position, focus, getCartesian3Scratch())
         var movementVector = Cartesian3.multiplyByScalar(direction, relativeAmount, direction)
         var endPosition = Cartesian3.add(focus, movementVector, focus)
 
