# 下一步修复与优化规划（2026-07-26 制定）

> 性质：规划文档（未实施）。基线 V3.4.36+，全部当日改动尚未 git 提交、未实机回归。
> 原则：先发现（P0 回归）→ 再修复（P1）→ 后优化（P2/P3）；每项附涉及文件与验收标准。

---

## P0 实机回归 + 疑似 bug 核查（首要，半天）

先跑 `LocalDev.bat`，按下表逐项核查。**前 5 条是今日改动中已预判的疑似 bug**，回归时优先打靶：

| # | 疑似问题 | 位置 | 核查/修复方向 | 验收标准 |
|---|---------|------|--------------|---------|
| 1 | ✅**已证实并修复**（2026-07-27）：偏好底图可选 `custom`/`local_tiles_preset` | store + `FloatingAccountPanel` | 双保险：store 新增 `isBasemapPreferenceSelectable`，`readCachedPreferredBasemap` 读取时过滤（已存脏值免疫，2D/3D 消费方自动生效）+ 偏好下拉选项过滤；断言 4/4 通过 | 实机复核：偏好下拉无两项 |
| 2 | ✅**已证实并修复**（2026-07-27）：账号中心全屏 z-index 降级，扫描确认与 LayerControlPanel/TopBar 弹层/HomeView(!important) 三处同层 2200 | theme.css + `FloatingAccountPanel` | theme.css 新增 `--z-overlay-top: 2400` 整屏覆盖档，全屏账号中心改用 | 实机复核：全屏时不被任何浮层遮挡 |
| 3 | **搜索/区划图层开放编辑的副作用**：搜索结果图层有自身高亮/标注逻辑，编辑高亮可能互相覆盖；行政区划边界被顶点编辑是否符合产品预期 | `useGeometryEdit.js`、搜索图层样式链 | 实测选中/取消后样式还原；若不符预期，将 search/district 加入排除名单（一行改动） | 编辑退出后原样式完整还原，无残留高亮 |
| 4 | **Chat 重新生成丢工具卡**：`prepareRegenerate` 截断到最后一条用户消息，其后的工具状态卡一并丢弃（设计使然），但重发不会重放已执行的地图操作 | `useChatSession.prepareRegenerate` + 容器 | 实测带工具调用的回复点"重新生成"：确认体验可接受（回复重生成、地图状态保持首轮结果）；不可接受则重发时携带工具结果摘要上下文 | 重新生成后消息流与地图状态一致、无悬空卡片 |
| 5 | **打字机与工具两轮的时序**：首轮 reply 含工具调用时走即时展示、纯回复走打字机；停止时机跨两轮，seq 守卫需实测覆盖"第二轮生成中停止" | `ChatPanelContent.dispatchSend/typewriterReveal` | 实测三场景：纯回复停止 / 工具执行中停止 / 二轮打字中停止 | 任一时点停止：输入立即解锁、无晚到内容插入、无卡死 loading |
| 6 | 六条核心回归（交接文档 §四）+ 各日志"测试方案"清单（约 40 项） | 全线 | 逐项勾选 | 全过或问题登记为 P1 |

## P1 确定性修复（回归后立即，按发现补充）

| # | 事项 | 说明 |
|---|------|------|
| 1 | P0 打靶出的实际 bug | 逐个修复 + 日志记录。🔧 实机回归首个发现已修（2026-07-26）：账号面板头部白字白底不可见——V3.4.28 遗留 `.blur-bg`(background:transparent) 源序覆盖 `.panel-header` 品牌渐变；删除死类 + 模板 3 处摘除，V3.4.55，日志 `26-07/2026-07-26/2026-07-26-account-panel-header-blurbg-fix.md` |
| 2 | ✅**部分完成**（2026-07-27）：单位制覆盖面扩展 | 已接入：公交路线里程/分段距离、驾车路线总里程、3D 漫游导航 HUD 距离（`distanceKm`→`distanceText` 带单位文本，模板去硬编码后缀）；余下：空间分析参数提示、坐标面板（输入型单位需换算逻辑，另行小改） |
| 3 | **git 分批提交** | 回归通过后按工作流分批（UI/UX 线 / 属性表线 / 配置线），用户执行 |

## P2 优化（一致性收尾，1~2 个会话）

| # | 事项 | 涉及 | 验收 |
|---|------|------|------|
| 1 | ❌**误判范围，撤销**（2026-07-27 扫描证实）：Routing 三面板非浮层面板家族（SidePanel 页签内容卡，无 0.95 框架签名、无绿色家族残留；蓝色系为驾车/选点功能语义色，双主题本一致） | — | 无需改动，规划更正 |
| 2 | 🎯**方向已定，待落地**（2026-07-26 用户拍板）：Cesium 工具面板接入走**组件域令牌收敛**——仿 toc-theme.css 建 cesium 域专用变量文件收编 56 处硬编码，保持暗色身份不并入 --panel-\*，零全局污染，为 P3-1 拆分铺路（决策记录见 V3.4.53 日志附录） | `CesiumToolPanel.vue`（56 处硬编码，暗色风格） | 落地会话：新建域变量文件 + 56 处机械替换 + 暗色观感零变化 |
| 3 | ✅**已完成**（2026-07-27）：`--toc-*` 与品牌令牌合流 | 24 个绿色系独立值派生自 --brand-*（上传区 11 项/边框徽章/页签/标题等），蓝主题自动联动；蓝覆盖块保留；剩余 3 个低饱和灰绿文本与 2 个已被覆盖项合理保留 | 实机复核：蓝主题 TOC 上传区/徽章/边框无残绿 |
| 4 | Chat 多会话管理 | 网页版标配缺口：会话列表/新建/切换/重命名（持久化结构 `chat:history:v1` 需升级为多会话 map，做好迁移） | 刷新后多会话可切换 |
| 5 | 账号中心统计骨架屏 | 首次加载 0 值闪烁 → 骨架占位（注意 30s 轮询不触发骨架） | 首开无 0 值闪烁 |

## P3 大项（单独立项，不混入日常修复）

| # | 事项 | 前置条件 |
|---|------|---------|
| 1 | 字号阶梯存量迁移（490+ 处 → `--fs-*` 六档） | 无；纯机械，按组件分批各自验证 |
| 2 | Chat 真流式输出（SSE） | 后端 agent_chat 支持 streaming；前端打字机改为消费真实流 |
| 3 | 完整 i18n（vue-i18n + 全站文案抽取） | 立项评估工作量；落地后接通语言偏好 |
| 4 | 3D 侧单位制/更多偏好项接入 | P1-2 完成后自然延伸 |

## 执行约定

- 每完成一项按 `Force_command.md` 写日志 + 版本记录；疑似 bug 证实后在本文档对应行标注结论（✅修复/❌误报）。
- P0 回归可由 Agent 借 Claude in Chrome 扩展实测截图完成（用户起 dev server 即可）。
