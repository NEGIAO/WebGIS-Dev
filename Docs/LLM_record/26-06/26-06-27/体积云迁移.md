你是一名精通 WebGIS 开发、三维图形学、WebGL 着色器（GLSL）以及前端工程化的资深架构师。
我现在需要你协助我完成一个跨三维引擎的功能重构与迁移任务。

【项目上下文与路径说明】
1. 源项目组件（Three.js 实现）：
   - 路径：D:\Dev\GitHub\tellux\README.md (及该仓库下的相关源码)
   - 功能：一个基于 Three.js 实现的“体积云（Volumetric Cloud）”模块。
2. 目标项目组件（Cesium.js 实现）：
   - 路径：D:\Dev\GitHub\WebGIS_Dev\frontend\src\components\Cesium\Cloud
   - 环境：Vue 3 + Vite + Cesium，控制面板使用 lil-gui 进行参数调试。

【核心任务】
你需要深入分析 tellux 中基于 Three.js 的体积云实现逻辑（如噪声纹理生成、Raymarching 光线步进算法、着色器 GLSL 逻辑等），然后将其完美迁移、重构为适合 Cesium 渲染架构的模块化组件，放置在目标路径的 `Cloud` 文件夹内，并最终接入 `lil-gui` 控制面板。

【第 1 步：分析与映射】
请首先针对 `D:\Dev\GitHub\tellux\` 仓库中与体积云（Volumetric Cloud）相关的核心代码进行深度分析。请向我输出一份分析报告，包含以下内容：
1. 核心逻辑解构：
   - 体积云的数据源是什么？（例如：是基于 3D 噪声纹理，还是解析式函数？）
   - 它的核心渲染原理是什么？（分析其 Raymarching 算法、光照计算、阴影/散射模拟等）。
   - 找出关键文件（如主 JS 类、Vertex Shader、Fragment Shader 路径及作用）。
2. 跨引擎技术映射（Three.js -> Cesium）：
   - Three.js 的 `Mesh`, `ShaderMaterial`, `Uniforms` 或 `CustomShader` 在 Cesium 中应该用什么 API 替代？（例如：是使用 Cesium 的 `PostProcessStage`（后处理），还是 `Primitive` + `CustomShader`，亦或是 `Appearance`？请对比优缺点并给出你推荐的架构方案）。
   - 针对 Cesium 的地球椭球体（WGS84）空间，体积云的包围盒（Bounding Box）和坐标转换（从局部空间到世界空间）该如何处理？

请先输出上述分析，在我确认后再进入下一阶段。


【第 2 步：模块化架构设计】
基于我们在阶段一确定的 Cesium 实现方案（假设采用 Primitive + CustomShader 或 PostProcessStage），请为 `D:\Dev\GitHub\WebGIS_Dev\frontend\src\components\Cesium\Cloud` 文件夹设计模块化的文件结构。

要求设计满足：
1. 高内聚低耦合：体积云的逻辑（纹理加载、着色器、生命周期管理）应完全封装在 `Cloud` 文件夹内。
2. 规范对齐：暴露清晰的初始化（init）、销毁（destroy）、显示/隐藏（show/hide）以及参数更新（updateUniforms）方法。
3. 请提供该文件夹下的目录树设计（例如：`index.ts`, `CloudManager.ts`, `CloudShader.glsl` 等），并简述每个文件的职责。

【第 3 步：核心代码编写】
现在，请开始编写核心重构代码。请将原 Three.js 的着色器和材质逻辑，翻译并重写为 Cesium 兼容的代码。

请完整输出：
1. 体积云核心管理类（如 `CloudManager.ts` 或 `VolumetricCloud.ts`）：
   - 负责创建 Cesium 的渲染对象（如 `Cesium.Primitive` 或后处理阶段）。
   - 负责传递 Uniforms 参数（云层高度、密度、散射系数、风向速度、噪声缩放等）。
2. GLSL 着色器代码：
   - 完整的顶点着色器和片元着色器（特别是 Raymarching 在 Cesium 自定义着色器中的实现，注意处理 Cesium 内置的自动 uniform，如自动深度纹理 `czm_globeDepthTexture` 或相机矩阵）。
   - 确保包含完整的噪声计算和光照物理模型，不要使用伪代码。

【第 4 步：控制面板与 GUI 接入】
体积云模块编写完成后，需要将其无缝接入到我的 Cesium 项目控制中心面板中，并使用 `lil-gui` 进行参数动态调试。

请提供接入代码示例：
1. 参数配置对象（Options）：定义所有可调节的体积云参数及其默认值、最大/最小值、步长（如 `density`: [0, 1, 0.01]）。
2. lil-gui 绑定逻辑：展示如何在控制面板组件中实例化 `lil-gui`，将控制器与体积云实例的 `updateUniforms` 方法或属性进行绑定，实现滑动条拖动时云层形态、光照实时响应。
3. Vue 3 组件生命周期钩子：确保在组件卸载（unmounted）时，不仅销毁 Cesium 中的云层 Primitive，同时正确销毁 `lil-gui` 实例，防止内存泄漏。


