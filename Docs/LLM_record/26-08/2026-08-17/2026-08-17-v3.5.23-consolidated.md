# 2026-08-17~19 V3.5.23 综合版本：Google 水系图层 · 瓦片解析通用化 · KML/KMZ 符号链路修复 · Sentinel-2 无云影像 · 管理后台移动端适配 · 出站浏览器特征头共享化

## 元信息

- **日期与时间**：2026-08-19 09:40（整合会话；功能实施于 2026-08-17 ~ 2026-08-19 多个会话）
- **任务等级**：L2（综合版本）
- **版本号**：V3.5.23（2026-08-17~19 的 V3.5.23–V3.5.28 六个增量按用户指令合并收敛为单一 V3.5.23）
- **本日志由 6 份分日志合并而成，原分日志保留**，内容全部收入本文（08-17 四份 + 08-18 五份 + 08-19 两份）

## 问题分析

### 核心症状（按功能域）

1. **Google 水系图层缺失**：Google 卫星影像无水系叠加层，用户无法在影像底图上查看河流/湖泊等水系要素。
2. **Google pb 风格瓦片 URL 无法代理**：`/proxy/gcj2wgs/` 与 `/proxy/wgs2gcj/` 无法代理 Google `maps/vt` 瓦片（`pb=!1m4!1m3!1i10!2i500!3i800!2m1!1e6` 格式），此前一律返回 400。
3. **天地图瓦片下载 418 拦截**：`download_xyz` 下载天地图瓦片时被 418 反爬拦截，且 UA/Referer 硬编码在下载器内部，其余瓦片出站面（gcj_rectify / api.proxy）仍裸奔。
4. **KML/KMZ 符号解析链路缺陷**：GBK 中文 KML/KMZ 被系统性误判为 UTF-16LE 而乱码（符号/样式全灭）；KMZ 内嵌资源 href 重写不完整；`loadKMZFallback` 字段名/返回结构全错触发必崩 TypeError。
5. **管理后台移动端失效**：顶部 Tab 在 touchstart 上无条件 `e.preventDefault()` 吞掉合成 click 导致点按无效；「数据管理」子页工具栏在窄屏横向溢出被裁切。
6. **HF 日志流 SSE 双重 data 前缀**：`stream_logs` 在 SSE 帧行带 `data: ` 前缀时 `json.loads` 必失败、原样透传导致前端拿到 `data: {json}` 字符串无法解析。

### 根本原因

1. 底图配置缺少 Google 水系叠加图层定义与预设。
2. `parse_tile_url` 的 path 模式用「每路径段只取第一个数字 + 取末 3 段」识别 x/y/z，而 pb 格式把 z/x/y 以 `!1i10!2i500!3i800` 前缀式内嵌在**同一个** path 段中——段内首数字是前缀标记（`!1m4` 的 4），解析必然失败。
3. 瓦片出站面（download_xyz / gcj_rectify / api.proxy）各自独立，UA 与 Referer 防盗链头未共享；天地图对非浏览器出站请求返回 418。
4. 编码判定原为「U+FFFD 替换字符最少」，但**任意字节流按 UTF-16 解码永不产生替换字符**——GBK 文本被误判为 UTF-16LE。
5. 移动端浏览器在 touchstart 被阻止后不再派发合成 click；`.db-toolbar` 的 `flex-wrap` 对「单组自身即超宽」的子分组无效。
6. HF 日志 API 的 SSE 帧行自带 `data: ` 前缀，后端未剥离直接尝试 JSON 解析。

### 受影响模块

底图配置（basemapConfig.ts / basemapPresets.ts）· 瓦片代理（proxy.py / url_template.py / tile_engine.py / fetch.py / http_headers.py）· 下载器（download.py）· KML/KMZ 加载（kmlLoader.js / useKmzLoader.js / textDecoder.js / kmlStyleParser.js / pathUtils.js / cesiumLayerNodeBuilder.ts）· 管理后台（AdminControlPanel.vue / LayerControlPanel.vue）· 注册页（RegisterView.vue）· 日志监控（monitor.py）· 配置（catalog.py / .env / .env.example / .env.local）· 文档（README / CHANGELOG / backend-structure.md / TODO）。

## 修改内容

### 一、Google 水系图层（纠偏叠加，原 V3.5.23）

1. `basemapConfig.ts` 新增 `imagery_google_water`（名称「Google水系(WGS)」，category=imagery / group=影像）。上游为 Google `lyrs=m`（道路图）瓦片，经 `apistyle=s.t:0|p.v:off,s.t:6|p.v:on` 过滤——关闭陆地（type 0）、仅保留水系特征（type 6），产出透明底的水系叠加瓦片；URL 通过 `gcj2wgsProxyUrl()` 由 `VITE_TILE_PROXY_BASE_URL` 派生（生产 = `https://negiao-webgis.hf.space/proxy/gcj2wgs/...`），完成 GCJ-02 → WGS-84 纠偏。沿用既有 `vector_Google_clean` 的 `%7C` 编码惯例；用户原始 URL 中的 `&&hl=zh-CN` 归一为单 `&`。
2. `basemapPresets.ts` 末尾追加 `imagery_google_water_preset`「Google影像水系」，stack = `['imagery_google', 'imagery_google_water']`（底部 Google 卫星影像 + 顶部水系叠加），末尾追加保证 URL 参数 `l` 的既有索引不变；默认预设（China Blender2）不动。
3. **SSOT 联动**：Cesium 描述符由 `getDescriptorById()` 自动派生，零额外维护；图层管理面板由 `LAYER_SOURCE_DEFINITIONS` 驱动自动出现。

### 二、瓦片解析通用化 + 出站浏览器特征头共享化（原 V3.5.24 / V3.5.25）

1. **Google pb 风格 URL 修复**：`parse_tile_url` 的 path 模式重写为「**全路径数字 token 扫描**」——`re.finditer` 取整条路径全部数字（含字符区间 spans），枚举全部有序三元组 (i<j<k)，zxy 序优先、其次 xyz 序做合法性校验（z≤30 且 x,y ≤ 2^z-1），合法候选中取 **x+y 最大**者（真实瓦片坐标量级必大于样式/版本等尾随参数数字，可同时兼容 `/z/x/y` 切片、`x{y}` 内嵌与 pb 前缀式）。重建改为按字符区间倒序替换，`! = { }` 等特殊字符零 urlencode 干扰。
2. **测试**：`backend/tests/test_url_template.py` 扩至 9 用例：内嵌多数字（pb 风格）、尾随垃圾数字、内嵌 xyz 序、用户报告 URL 端到端、format/query/path 三模式回归、无 xyz 报错。全量后端 34 个测试通过。
3. **天地图 418 修复**：`download_xyz/tile_engine.py` 注入浏览器 UA + 天地图 Referer 防盗链头，418 反爬拦截不再重试直接放弃。
4. **出站浏览器特征头共享化**：新增 `backend/utils/http_headers.py`（`BROWSER_USER_AGENT` 常量 + `REFERER_BY_DOMAIN` 白名单 + `referer_headers_for()`），`download_xyz` / `gcj_rectify` / `api.proxy` 三瓦片出站面共用；`api/proxy.py` 的 `_build_proxy_request_headers` 改为按上游域名白名单附加 Referer（白名单源不透传客户端 Referer）。
5. **download_xyz 内网放行开关**：新增 `DOWNLOAD_ALLOW_PRIVATE_HOSTS` 配置（默认 false），与 `/proxy` 面的 `PROXY_ALLOW_PRIVATE_HOSTS` 解耦，避免共用配置互相影响。

### 三、KML/KMZ 符号解析链路修复（原 V3.5.25）

1. **编码探测加固（本轮最大发现）**：`textDecoder.js` 与 `useKmzLoader.js` 的编码判定原为「U+FFFD 替换字符最少」，但**任意字节流按 UTF-16 解码永不产生替换字符**——GBK 中文 KML/KMZ 被系统性误判为 UTF-16LE 而乱码（符号/样式全灭）。改为：BOM 权威判定（UTF-8/UTF-16LE/UTF-16BE）+ 无 BOM 打分制（U+FFFD 重罚 ×10000 + C0 控制字符罚分 + 字节级 0x00 支撑校验——真 UTF-16 的 ASCII 标记必然产生 0x00，LE 在奇位、BE 在偶位，据此排除单字节编码误读并区分无 BOM 字节序）。
2. **KML 加载重构**：`kmlLoader.js` 不再把 blob URL 直接交给 `Cesium.KmlDataSource`（其内部固定 UTF-8 读取），改为 ArrayBuffer → 多编码解码 → 文本 Blob 加载。
3. **KMZ 统一手动管线**：`extractKmlFromKmz(rewriteResourceBlobUrls=true)`——doc.kml 智能选择 + 多编码解码 + 内嵌资源 href **全量**重写为 blob URL（原实现仅重写图片且无容错；新 `lookupZipEntry` 三级容错：精确 → 大小写不敏感 → URL 解码，兼容 `./`、`../`、反斜杠、`%20` 等变体）；删除损坏的 `loadKMZFallback`（其字段名/返回结构全错，触发必崩 TypeError）与「原生优先」双路径；重写产生的 blob URL 登记 `record.blobUrls`，由移除/清空数据源时统一回收。
4. **`normalizePath` 修复**：`/^\.\/?/` → `/^\.\//`，`../x` 前缀不再被剥成 `./x`（上跳相对路径解析错位的共享缺陷，2D 管线同受益）。
5. **TOC**：`TYPE_LABELS` 补 `kmz: 'KMZ'`，三维数据分组格式标签正常化。

### 四、Sentinel-2 无云年度影像图层与预设（原 V3.5.26）

1. **新增 10 个 Sentinel-2 无云年度图层**（`imagery_s2_cloudless_2016` ~ `_2025`，EOX tiles.maps.eox.at WMTS 公开服务零 token）：2016 对应官方聚合层 `s2cloudless_3857`（Capabilities 标题即 "Sentinel-2 cloudless layer for 2016"，经实测 2016 无独立年度层，400 验证），2017~2025 对应年度层 `s2cloudless-YYYY_3857`（Capabilities 逐层验证 200）；URL 采用 `{z}/{x}/{y}` 模板直连（GoogleMapsCompatible 矩阵集）。
2. **图层生成器收敛**：10 个年度条目结构相同，`basemapConfig.ts` 新增 `buildS2CloudlessDef(year)` 生成器 + `EOX_WMTS_URL(layer)` 模板函数，url 与 createSource 共用同一模板串，杜绝两字段漂移（文件内已有 buildTiandituUrl 等函数先例）。
3. **新增 10 个底图预设**（`imagery_s2_cloudless_YYYY_preset`，label「Sentinel无云YYYY」，stack = 年度图层 + `label_tianditu` 注记叠加），追加在 `BASEMAP_PRESETS` 尾部，既有 l 索引编号不受影响。

### 五、管理后台移动端适配（原 V3.5.27 / V3.5.28）

1. **顶部 Tab 移动端点按无效修复**：`AdminControlPanel.vue` 的 `onTabsNavDragStart` 在 `touchstart` 上无条件 `e.preventDefault()`——移动端浏览器在 touchstart 被阻止后不再派发合成 click，按钮 `@click` 永远不触发（桌面端 mousedown 的 preventDefault 不吞 click，故仅移动端中招）。改为位移阈值（10px）判定 + 捕获阶段 click 处理器吞掉拖拽残留 click；`.tab-btn` 由 `flex: 1 1 0; min-width: 0` 改为 `flex: 1 1 auto; min-width: 180px`（移动端 ≤768px 为 150px），窄屏可横向滑动、文字不再截断。
2. **「数据管理」子页移动端 UI 适配**：`.db-toolbar` 的 `flex-wrap` 对「单组自身即超宽」的子分组无效——切表/统计/分页三个分组各自单行排布（如分页组 select+上一页+页码+下一页+导出 ≈ 320px+），375px 手机扣外层 padding 后可用宽度仅约 287px，必然横向溢出被裁切。`@media (max-width: 768px)` 内新增：工具栏改竖排堆叠；切表下拉弹性撑满行宽；搜索框独占一行；页码信息居中成行；导出按钮独占一行靠右；卡片头（标题+刷新表）改纵向堆叠；表格容器 `max-height` 改 60vh（原固定 480px）。`#admin-panel-database` 前缀限定 card-header 规则只作用于本子页；桌面端样式零改动。

### 六、顺带

1. **HF 日志流 SSE 双重 data 前缀修复**：`backend/api/monitor.py` 的 `stream_logs` 在 SSE 帧行带 `data: ` 前缀时先剥离再尝试 JSON 解析，否则 `json.loads` 必失败、原样透传导致前端拿到 `data: {json}` 字符串无法解析。
2. **注册页确认登录按钮**：`RegisterView.vue` 的「确认登录」按钮由 `type="button"` + `@click="handleSubmit"` 改为 `type="submit"`（表单原生提交，消除重复触发风险）。
3. **图层管理面板下拉高度**：`LayerControlPanel.vue` 下拉菜单 `max-height` 450px → 480px（适配新增底图预设数量）。
4. **底图注记切换**：`basemapPresets.ts` 多个预设的注记图层由 `label_tianditu` 切换为 `Omap_label`（奥维注记，`basemapConfig.ts` 新增 `Omap_label` 图层定义 + `terrain_omap_contour` 奥维等高线图层 + `imagery_tianditu_omap_contour_preset` 天地图影像+等高线预设）。

## 验证

- 后端 `py_compile` 通过（monitor / proxy / catalog / download / tile_engine / fetch / url_template / http_headers）。
- `backend/tests/test_url_template.py` 9 用例全过，全量后端 34 个测试通过。
- 前端 `tsc --noEmit` 通过（basemapConfig / basemapPresets / cesiumLayerNodeBuilder）。
- ESLint 前端改动文件零告警。
- 双门禁通过（配置 key 登记 / 结构树漂移检查）。

## 遗留与风险

- **`kmlParser.ts` 死代码仍含旧式编码判定**：零引用（GBK 误判缺陷与 textDecoder 同源），清理或同步新启发式（已记 TODO）。
- **`kmz` 类型未列入 `OPACITY_SUPPORTED_TYPES`**：TOC 中 KMZ 数据源无透明度滑杆（已记 TODO）。
- **AdminControlPanel Globe 图标未导入**：模板第 1250 行 `<Globe :size="16" />`（底图配置卡片标题），但 script 导入清单无 `Globe`，运行时会警告 "Failed to resolve component: Globe" 且图标渲染为空（已记 TODO）。
- **`agentMapPresets.js:4` 注释残留旧编号「V3.5.28」**：该文件非本次暂存范围（已提交文件），其注释沿用旧增量编号，与新版本体系不一致，未擅自修改，记入待办观察。

## 变更清单

| 文件 | 变更 |
|------|------|
| `README.md` | 版本号三处更新至 V3.5.23 + 演进表合并 |
| `Docs/Guide/CHANGELOG.md` | 顶部合并为 V3.5.23 综合条目 |
| `Docs/Guide/backend-structure.md` | tests/ 结构树登记新测试文件 + utils/ 登记 http_headers.py |
| `backend/api/monitor.py` | SSE 帧行 data 前缀剥离修复 |
| `backend/api/proxy.py` | `_build_proxy_request_headers` 按上游域名白名单附加 Referer |
| `backend/config/catalog.py` | 登记 `DOWNLOAD_ALLOW_PRIVATE_HOSTS` |
| `backend/download_xyz/download.py` | 内网放行开关 + 注释更新 |
| `backend/download_xyz/tile_engine.py` | UA + Referer + 418 不重试 |
| `backend/gcj_rectify/fetch.py` | UA + Referer 注入 |
| `backend/gcj_rectify/url_template.py` | 全路径数字 token 扫描通用化 |
| `backend/tests/test_url_template.py` | 新增 9 用例 |
| `backend/utils/http_headers.py` | **新增** 出站浏览器特征头共享单点 |
| `frontend/src/app/RegisterView.vue` | 确认登录按钮 type=submit |
| `frontend/src/domains/cesium/composables/dataImport/loaders/kmlLoader.js` | KML/KMZ 加载重构 |
| `frontend/src/domains/cesium/stores/cesiumLayerNodeBuilder.ts` | TYPE_LABELS 补 kmz 标签 |
| `frontend/src/domains/common/basemap/basemapPresets.ts` | Google 水系 + Sentinel 10 预设 + Omap_label 切换 |
| `frontend/src/domains/common/data-import/parsers/kmlStyleParser.js` | 样式解析修复 |
| `frontend/src/domains/common/data-import/textDecoder.js` | 编码探测加固 |
| `frontend/src/domains/common/data-import/useKmzLoader.js` | KMZ 资源重写 + 编码探测 |
| `frontend/src/domains/common/user/components/AdminControlPanel.vue` | 移动端 Tab 修复 + 数据管理子页适配 |
| `frontend/src/domains/common/utils/pathUtils.js` | normalizePath 修复 |
| `frontend/src/domains/ol/basemap/constants/basemapConfig.ts` | Google 水系 + Sentinel 生成器 + Omap_label + 等高线 |
| `frontend/src/domains/ol/layer/components/LayerControlPanel.vue` | 下拉高度 450→480px |
| `.env` / `.env.example` / `.env.local` | 登记 `DOWNLOAD_ALLOW_PRIVATE_HOSTS` + `PROXY_ALLOWED_HOSTS=*` |
| `Docs/TODO/bugfix-optimization-plan.md` | 登记遗留项 |